var typed = new Typed(".multiple-text", {
    strings: ["Frontend Developer", "CP guy", "philantrophist",
        "Gamer", "Techy guy", "GEEK"
    ],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    Loop: true
})

let sections = document.querySelector('section');
let navlinks = document.querySelector('header nav a');